name: Bygg Windows .exe + portabel zip

# Kör automatiskt vid push till main (rullande "latest"-release), manuellt via
# Actions-fliken → "Run workflow", eller pusha en tagg v* (t.ex. v0.2.0) för en
# versionerad release.
on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - 'v*'

permissions:
  contents: write   # krävs för att skapa/uppdatera Releases

jobs:
  windows-exe:
    runs-on: ubuntu-latest
    steps:
      - name: Checka ut koden
        uses: actions/checkout@v4

      - name: Sätt upp Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Bygg Windows amd64 .exe (CGO-fritt, ren Go, utan konsolfönster)
        run: |
          # -H windowsgui = GUI-subsystem → inget svart konsolfönster vid dubbelklick.
          GOOS=windows GOARCH=amd64 CGO_ENABLED=0 \
            go build -ldflags="-s -w -H windowsgui" -o cert-renamer.exe ./cmd/cert-renamer
          ls -lh cert-renamer.exe

      - name: Bygg cert-renamer-v2.exe (parallellt V2-spår)
        run: |
          # Samma mönster som V1: GUI-subsystem, CGO-fritt, fristående binär.
          GOOS=windows GOARCH=amd64 CGO_ENABLED=0 \
            go build -ldflags="-s -w -H windowsgui" -o cert-renamer-v2.exe ./cmd/cert-renamer-v2
          ls -lh cert-renamer-v2.exe

      - name: Bygg diagnostikverktyget monitor-probe.exe (konsol, för felsökning av djuplänkar)
        run: |
          # Inget -H windowsgui: probe-verktyget ska ha konsol (skriver ut resultat).
          GOOS=windows GOARCH=amd64 CGO_ENABLED=0 \
            go build -ldflags="-s -w" -o monitor-probe.exe ./cmd/monitor-probe
          ls -lh monitor-probe.exe

      - name: Paketera portabel zip
        run: |
          mkdir -p pkg
          cp cert-renamer.exe pkg/
          cp cert-renamer-v2.exe pkg/
          cp monitor-probe.exe pkg/
          cat > "pkg/LÄS-MIG.txt" <<'TXT'
          Cert Renamer — portabel (Windows x64)

          1. Packa upp denna zip var som helst (t.ex. Skrivbordet eller en USB-sticka).
          2. Dubbelklicka cert-renamer.exe. En webbläsare öppnas mot appen (loopback).
          3. Första gången: öppna Inställningar (kugghjulet) och klistra in din
             Anthropic API-nyckel + välj inbox-mapp.

          Inställningar och databasen (cert-renamer.db) sparas i
          %APPDATA%\cert-renamer\ — inte bredvid exe:n. Exe:n är fristående
          (ingen installation, inga beroenden).

          Appen kör utan eget fönster (bara webbläsarfliken syns). För att
          avsluta: Aktivitetshanteraren -> hitta "cert-renamer.exe" -> Avsluta
          aktivitet. (Servern fortsätter annars köra i bakgrunden.)

          --- V2 (cert-renamer-v2.exe) ---

          V2 ar det nya parallella sparet: databasen ar sanningen och certet
          dops om forst nar du trycker Spara. Kors sida vid sida med V1: delar
          config.json (API-nyckel, Monitor, inbox) men har EGEN databas
          (cert-renamer-v2.db) och egna mappar (<inbox>\v2\store certlager,
          <inbox>\v2\out sparade cert).

          OBS: kor bara EN binars mailintag at gangen (bada pollar samma
          inbox) — hall V1-workern "Av" nar V2:s intag ar pa.

          Engangsimport av V1-historiken (kor i en kommandotolk):
              cert-renamer-v2.exe -import-v1
          TXT
          cd pkg
          zip -r ../cert-renamer-windows-amd64.zip .
          cd ..
          ls -lh cert-renamer-windows-amd64.zip

      - name: Bestäm release-tagg
        id: meta
        run: |
          if [ "${{ github.ref_type }}" = "tag" ]; then
            echo "tag=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          else
            echo "tag=latest" >> "$GITHUB_OUTPUT"
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Publicera Release med exe + zip
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${{ steps.meta.outputs.tag }}"
          # Rullande "latest": ta bort gammal release + tagg så assets uppdateras.
          if [ "$TAG" = "latest" ]; then
            gh release delete latest --yes --cleanup-tag || true
          fi
          ARGS=(--title "cert-renamer $TAG" --target "$GITHUB_SHA"
                --notes "Automatisk Windows-build. Ladda ner **cert-renamer-windows-amd64.zip**, packa upp och dubbelklicka cert-renamer.exe. (Bygg-SHA: ${GITHUB_SHA:0:7})")
          if [ "${{ steps.meta.outputs.prerelease }}" = "true" ]; then
            ARGS+=(--prerelease)
          fi
          gh release create "$TAG" cert-renamer-windows-amd64.zip cert-renamer.exe cert-renamer-v2.exe monitor-probe.exe "${ARGS[@]}"
          echo "Release-URL:"
          gh release view "$TAG" --json url -q .url
